using UnityEngine;

namespace States
{
     enum PlayerStates
        {
            Idle,
            Move,
        }

     enum EnemyStates
     {
         Idle,
         Move,
         Attack
     }
}